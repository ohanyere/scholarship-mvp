apiVersion: v1
kind: Service
metadata:
  name: scholarship-platform-api
  labels:
    app.kubernetes.io/name: scholarship-platform-api
spec:
  selector:
    app.kubernetes.io/name: scholarship-platform-api
  ports:
    - name: http
      port: 80
      targetPort: http
