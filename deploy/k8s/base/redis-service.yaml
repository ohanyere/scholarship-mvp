apiVersion: v1
kind: Service
metadata:
  name: scholarship-platform-redis
  labels:
    app.kubernetes.io/name: scholarship-platform-redis
spec:
  selector:
    app.kubernetes.io/name: scholarship-platform-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
